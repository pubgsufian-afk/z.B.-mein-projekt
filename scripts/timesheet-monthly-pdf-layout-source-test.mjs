import fs from 'node:fs'
import assert from 'node:assert/strict'

const source = fs.readFileSync('netlify/functions/timesheet-monthly-reports.mts', 'utf8')
assert.match(source, /page\.drawText\('Stundenzettel'/)
assert.doesNotMatch(source, /Arbeitszeitenbericht/)
assert.match(source, /Arbeitnehmer:/)
assert.match(source, /Datum.*Startzeit.*Endzeit.*Pause.*Arbeitsstunden/s)
assert.match(source, /rollupDailyTimesheetRows/)
assert.match(source, /pauseDisplay/)
assert.doesNotMatch(source, /Tätigkeit \/ Einsatzort/)
assert.doesNotMatch(source, /statusText|activityLocation/)
assert.match(source, /Platz für weitere Anmerkungen/)
assert.match(source, /headerGreen/)
assert.match(source, /totalOrange/)
assert.match(source, /isWeekend\(item\.date\)/)
assert.match(source, /\/habun-logo-pdf\.png/)
assert.match(source, /opacity:\s*1/)
assert.match(source, /green/i)
assert.match(source, /orange/i)
assert.doesNotMatch(source, /attendance_events|\/api\/attendance|Erfasst/)
console.log('timesheet monthly PDF layout source contract passed')
