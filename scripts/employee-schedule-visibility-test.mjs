import assert from 'node:assert/strict'
import { readFile } from 'node:fs/promises'

const sessionSource = await readFile('netlify/functions/session.mts', 'utf8')
const appSource = await readFile('frontend/src/App.jsx', 'utf8')
const scheduleSource = await readFile('netlify/functions/schedule-v2-neon.mts', 'utf8')
const repositorySource = await readFile('netlify/functions/_shared/schedule-neon-repository.mts', 'utf8')
const browserSource = await readFile('tests/e2e/unified-portal.spec.mjs', 'utf8')

assert.ok(sessionSource.includes('userId: data.userId || data.id'))
assert.ok(scheduleSource.includes("const ownEntries = await listScheduleShifts({ from, to, employeeUserId: current.userId, publishedOnly: true })"))
assert.ok(scheduleSource.includes('let activeEmployees = await listActiveScheduleEmployees()'))
assert.ok(scheduleSource.includes('let currentEmployee = activeEmployees.find'))
assert.ok(scheduleSource.includes('if (!currentEmployee) {'))
assert.ok(scheduleSource.includes('let sameNameEmployees = activeEmployees.filter'))
assert.ok(scheduleSource.includes('const hasOtherSameNameEntry = publishedEntries.some'))
assert.ok(scheduleSource.includes('if (!hasOtherSameNameEntry) return ownEntries'))
assert.ok(scheduleSource.includes('if (sameNameEmployees.length !== 1) return ownEntries'))
assert.ok(scheduleSource.includes("publishedEntries = await listScheduleShifts({ from, to, publishedOnly: true })"))
assert.ok(scheduleSource.includes('new Map([...ownEntries, ...sameNameEntries].map((entry) => [entry.id, entry])).values()'))
assert.ok(scheduleSource.includes('new Map([...refreshedOwnEntries, ...sameNameEntries].map((entry) => [entry.id, entry])).values()'))
assert.ok(scheduleSource.includes('await syncActiveEmployees()'))
assert.ok(!scheduleSource.includes('listEmployeeScheduleShifts'))
assert.ok(!repositorySource.includes('export async function listEmployeeScheduleShifts'))
assert.ok(appSource.includes('const visibleEntries = entries'))
assert.ok(!appSource.includes('const employeeSessionUserId = session.userId || session.id'))
assert.ok(browserSource.includes("role === 'employee'"))
assert.ok(browserSource.includes("{ id: 'legacy-session-anna'"))
assert.ok(browserSource.includes("entry.employeeUserId === 'employee-anna' && entry.status === 'published'"))

console.log('Employee schedule visibility tests passed')
