import assert from 'node:assert/strict'
import { readFile, writeFile } from 'node:fs/promises'

const path = 'netlify/functions/schedule-assistant.mts'
let source = await readFile(path, 'utf8')

if (!source.includes('requestedEmployeeUserId')) {
  const oldBlock = `  const resolved = resolveAssistantEmployee(input.employeeName, employees)\n  if (resolved.status === 'ambiguous') {\n    return {\n      index,\n      employeeName: text(input.employeeName),\n      status: 'ambiguous',\n      candidates: resolved.candidates.map((candidate) => candidate.fullName),\n    }\n  }\n\n  let employee: { userId: string; fullName: string }\n  if (resolved.status === 'matched' && resolved.employee) {\n    employee = resolved.employee\n  } else if (allowUnregistered && resolved.status === 'not_found') {\n    const fullName = text(input.employeeName)\n    const userId = provisionalEmployeeUserId(fullName)\n    if (!userId) {\n      return { index, employeeName: fullName, status: 'invalid', message: 'Mitarbeitername fehlt.' }\n    }\n    employee = { userId, fullName }\n  } else {\n    return { index, employeeName: text(input.employeeName), status: 'not_found' }\n  }`

  assert.ok(source.includes(oldBlock), 'Schedule publish employee resolution anchor missing')

  const newBlock = `  const requestedEmployeeUserId = text((input as unknown as Record<string, unknown>).employeeUserId)\n  let employee: { userId: string; fullName: string }\n\n  if (requestedEmployeeUserId) {\n    const candidate = employees.find((candidate) => candidate.userId === requestedEmployeeUserId)\n    if (!candidate) {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'not_found',\n        message: 'Mitarbeiter-ID wurde nicht gefunden.',\n      }\n    }\n    if (text(input.employeeName).toLocaleLowerCase('de') !== candidate.fullName.trim().toLocaleLowerCase('de')) {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'invalid',\n        message: 'Mitarbeiter-ID und Mitarbeitername passen nicht zusammen.',\n      }\n    }\n    employee = candidate\n  } else {\n    const resolved = resolveAssistantEmployee(input.employeeName, employees)\n    if (resolved.status === 'ambiguous') {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'ambiguous',\n        candidates: resolved.candidates.map((candidate) => candidate.fullName),\n      }\n    }\n\n    if (resolved.status === 'matched' && resolved.employee) {\n      employee = resolved.employee\n    } else if (allowUnregistered && resolved.status === 'not_found') {\n      const fullName = text(input.employeeName)\n      const userId = provisionalEmployeeUserId(fullName)\n      if (!userId) {\n        return { index, employeeName: fullName, status: 'invalid', message: 'Mitarbeitername fehlt.' }\n      }\n      employee = { userId, fullName }\n    } else {\n      return { index, employeeName: text(input.employeeName), status: 'not_found' }\n    }\n  }`

  source = source.replace(oldBlock, newBlock)
}

if (!source.includes('SAFE_SCHEDULE_NAME_PREFLIGHT_APPLIED')) {
  const importAnchor = '  resolveAssistantEmployee,\n  resolveAssistantWorksite,\n'
  assert.ok(source.includes(importAnchor), 'Safe schedule resolver import anchor missing')
  source = source.replace(
    importAnchor,
    '  resolveAssistantEmployee,\n  resolveAssistantSchedulePerson,\n  resolveAssistantWorksite,\n',
  )

  const signatureAnchor = `  employees: AssistantDirectoryEmployee[],\n  worksites: AssistantWorksite[],\n  allowUnregistered: boolean,\n) {`
  assert.ok(source.includes(signatureAnchor), 'Safe schedule publish signature anchor missing')
  source = source.replace(
    signatureAnchor,
    `  employees: AssistantDirectoryEmployee[],\n  worksites: AssistantWorksite[],\n  employee: { userId: string; fullName: string },\n) {`,
  )

  const unsafeResolutionBlock = `  const requestedEmployeeUserId = text((input as unknown as Record<string, unknown>).employeeUserId)\n  let employee: { userId: string; fullName: string }\n\n  if (requestedEmployeeUserId) {\n    const candidate = employees.find((candidate) => candidate.userId === requestedEmployeeUserId)\n    if (!candidate) {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'not_found',\n        message: 'Mitarbeiter-ID wurde nicht gefunden.',\n      }\n    }\n    if (text(input.employeeName).toLocaleLowerCase('de') !== candidate.fullName.trim().toLocaleLowerCase('de')) {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'invalid',\n        message: 'Mitarbeiter-ID und Mitarbeitername passen nicht zusammen.',\n      }\n    }\n    employee = candidate\n  } else {\n    const resolved = resolveAssistantEmployee(input.employeeName, employees)\n    if (resolved.status === 'ambiguous') {\n      return {\n        index,\n        employeeName: text(input.employeeName),\n        status: 'ambiguous',\n        candidates: resolved.candidates.map((candidate) => candidate.fullName),\n      }\n    }\n\n    if (resolved.status === 'matched' && resolved.employee) {\n      employee = resolved.employee\n    } else if (allowUnregistered && resolved.status === 'not_found') {\n      const fullName = text(input.employeeName)\n      const userId = provisionalEmployeeUserId(fullName)\n      if (!userId) {\n        return { index, employeeName: fullName, status: 'invalid', message: 'Mitarbeitername fehlt.' }\n      }\n      employee = { userId, fullName }\n    } else {\n      return { index, employeeName: text(input.employeeName), status: 'not_found' }\n    }\n  }\n\n`
  assert.ok(source.includes(unsafeResolutionBlock), 'Unsafe schedule employee resolution block missing')
  source = source.replace(unsafeResolutionBlock, '')

  const publishLoopAnchor = `      const worksites = await activePortalWorksites()\n      const results = []\n      for (let index = 0; index < shifts.length; index += 1) {\n        const input = shifts[index]\n        if (!input || typeof input !== 'object' || Array.isArray(input)) {\n          results.push({ index, status: 'invalid', message: 'Dienst ist ungültig.' })\n          continue\n        }\n        results.push(await publishOne(input as PublishInput, index, requestId, employees, worksites, allowUnregistered))\n      }`
  assert.ok(source.includes(publishLoopAnchor), 'Unsafe schedule publish loop anchor missing')

  const safePublishLoop = `      const approvedUnregisteredNames = Array.isArray(body.approvedUnregisteredNames)\n        ? body.approvedUnregisteredNames.map(text).filter(Boolean).slice(0, MAX_BATCH)\n        : []\n      const worksites = await activePortalWorksites()\n      const historicalShifts = allowUnregistered ? await listScheduleShifts() : []\n      const provisionalByUserId = new Map<string, { userId: string; fullName: string }>()\n      for (const shift of historicalShifts) {\n        if (!isProvisionalEmployeeUserId(shift.employeeUserId)) continue\n        if (!provisionalByUserId.has(shift.employeeUserId)) {\n          provisionalByUserId.set(shift.employeeUserId, {\n            userId: shift.employeeUserId,\n            fullName: shift.employeeName,\n          })\n        }\n      }\n      const provisionalEmployees = [...provisionalByUserId.values()]\n      const resolvedEmployees = new Map<number, { userId: string; fullName: string }>()\n      const preflightResults: Array<Record<string, unknown>> = []\n      let preflightFailed = false\n\n      for (let index = 0; index < shifts.length; index += 1) {\n        const input = shifts[index]\n        if (!input || typeof input !== 'object' || Array.isArray(input)) {\n          preflightFailed = true\n          preflightResults.push({ index, status: 'invalid', message: 'Dienst ist ungültig.' })\n          continue\n        }\n\n        const publishInput = input as PublishInput\n        const validation = validateAssistantShiftInput(publishInput)\n        if (!validation.ok) {\n          preflightFailed = true\n          preflightResults.push({ index, employeeName: text(publishInput.employeeName), status: 'invalid', message: validation.message })\n          continue\n        }\n\n        const worksiteResolution = resolveAssistantWorksite(publishInput.location, worksites)\n        if (worksiteResolution.status !== 'matched' || !worksiteResolution.worksite) {\n          preflightFailed = true\n          preflightResults.push({\n            index,\n            employeeName: text(publishInput.employeeName),\n            status: \`location_\${worksiteResolution.status}\`,\n            location: text(publishInput.location),\n          })\n          continue\n        }\n\n        const personResolution = resolveAssistantSchedulePerson(\n          publishInput.employeeName,\n          employees,\n          allowUnregistered ? provisionalEmployees : [],\n          allowUnregistered ? approvedUnregisteredNames : [],\n        )\n        const requestedEmployeeUserId = text((publishInput as unknown as Record<string, unknown>).employeeUserId)\n        let employee: { userId: string; fullName: string } | null = null\n\n        if (personResolution.status === 'matched' && personResolution.employee) {\n          employee = personResolution.employee\n        } else if (personResolution.status === 'approved_unregistered') {\n          const userId = provisionalEmployeeUserId(personResolution.fullName)\n          if (userId) employee = { userId, fullName: personResolution.fullName }\n        } else {\n          preflightFailed = true\n          preflightResults.push({\n            index,\n            employeeName: text(publishInput.employeeName),\n            status: personResolution.status,\n            candidates: personResolution.candidates.map((candidate) => candidate.fullName),\n          })\n          continue\n        }\n\n        if (!employee) {\n          preflightFailed = true\n          preflightResults.push({ index, employeeName: text(publishInput.employeeName), status: 'invalid' })\n          continue\n        }\n\n        if (requestedEmployeeUserId && employee.userId !== requestedEmployeeUserId) {\n          preflightFailed = true\n          preflightResults.push({\n            index,\n            employeeName: text(publishInput.employeeName),\n            status: 'invalid',\n            message: 'Mitarbeiter-ID und Mitarbeitername passen nicht zusammen.',\n          })\n          continue\n        }\n\n        resolvedEmployees.set(index, employee)\n        preflightResults.push({ index, employeeName: employee.fullName, status: 'ready' })\n      }\n\n      if (preflightFailed) {\n        return json({\n          integration: 'Dienstplan-Assistent',\n          role: 'scheduler',\n          requestId,\n          directoryDiagnostics,\n          preflightFailed: true,\n          results: preflightResults,\n        }, 200)\n      }\n\n      const results = []\n      for (let index = 0; index < shifts.length; index += 1) {\n        const input = shifts[index] as PublishInput\n        const employee = resolvedEmployees.get(index)\n        if (!employee) {\n          results.push({ index, employeeName: text(input.employeeName), status: 'invalid', message: 'Mitarbeiterauflösung fehlt.' })\n          continue\n        }\n        results.push(await publishOne(input, index, requestId, employees, worksites, employee))\n      }`

  source = source.replace(publishLoopAnchor, safePublishLoop)
  source += '\n// SAFE_SCHEDULE_NAME_PREFLIGHT_APPLIED\n'
}

assert.match(source, /requestedEmployeeUserId/)
assert.match(source, /resolveAssistantSchedulePerson/)
assert.match(source, /preflightFailed: true/)
assert.doesNotMatch(source, /allowUnregistered && resolved\.status === 'not_found'/)

await writeFile(path, source)
console.log('Schedule safe name preflight patch applied')
